{
  "name": "ezbid-root",
  "version": "1.0.0",
  "description": "EzBid - AI-Powered Opportunity Finder and Bid Manager for Flagstaffe",
  "main": "backend/index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node backend/index.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run copy:build",
    "copy:build": "node copy-build.js",
    "start": "node backend/index.js",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:coverage": "npm run test:coverage --prefix backend && npm run test:coverage --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
